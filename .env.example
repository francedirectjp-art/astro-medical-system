# これを .env にコピーして埋める（.env は .gitignore 済み）。
# 本番(Railway)では Settings -> Variables に同じキーを登録する。

# Claude (Anthropic) API key — 鑑定(一気通貫)に必須
ANTHROPIC_API_KEY=sk-ant-...

# 鑑定に使うモデル（既定: Sonnet 4.6）
READING_MODEL=claude-sonnet-4-6

# 1ターンあたりの最大出力トークン（任意）
# READING_MAX_TOKENS=4000
